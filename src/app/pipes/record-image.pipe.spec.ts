import { RecordImagePipe } from './record-image.pipe';

describe('RecordImagePipe', () => {
  it('create an instance', () => {
    const pipe = new RecordImagePipe();
    expect(pipe).toBeTruthy();
  });
});
